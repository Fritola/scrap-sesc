name: Atualizar Cardápios

on:
  schedule:
    - cron: "0 10 * * 0" 
    - cron: "0 13 * * 0" 
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Chamar endpoint protegido
        run: |
          curl -X POST https://scrap-sesc.onrender.com/menu/update-all \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
