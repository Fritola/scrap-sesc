generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model WeeklyMenu {
  id        Int      @id @default(autoincrement())
  unit      String
  slug      String   @unique
  data      Json
  updatedAt DateTime @updatedAt
}